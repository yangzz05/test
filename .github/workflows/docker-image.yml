name: mosdnsui

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  release-and-tag:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: mosdnsui
      run: docker build . --file Dockerfile --tag mosdnsui:$(date +%s)
